use crate::components::{
    Active, Button, HealthBar, Mousehandler, Player, Position, Rectangle, Sprite,
};
use specs::prelude::*;

use crate::{HEIGHT, WIDTH};

pub fn setup_main_menu(world: &mut World) {
    
}